# Supabase
# URL du projet Supabase (Settings > API)
SUPABASE_URL=https://your-project.supabase.co
# Clé anon (publique, utilisée côté client pour Auth et requêtes)
SUPABASE_KEY=your-anon-key
# Clé service role (SECRÈTE, serveur uniquement — vérification JWT dans /api/admin/*)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# API / S3
# URL de base du bucket S3 (images)
NUXT_PUBLIC_API_URL=https://plaumondpicture.s3.eu-west-3.amazonaws.com

# AWS S3 (upload côté serveur)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=eu-west-3
S3_BUCKET=plaumondpicture

# Optionnel — pour scripts/verify-admin-auth.ts (ne pas committer en prod)
# ADMIN_EMAIL=patrick@example.com
# ADMIN_PASSWORD=***

# Optionnel
# NUXT_PUBLIC_GTAG_ID=G-XXXXX

# Google Analytics 4 (Dashboard Admin)
# Property ID numérique depuis GA4 Admin > Property Settings
# GA4_PROPERTY_ID=123456789
# JSON du Service Account (sur une seule ligne, entre quotes simples)
# GOOGLE_APPLICATION_CREDENTIALS_JSON='{"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}'
